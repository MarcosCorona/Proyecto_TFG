<UserControl x:Class="Proyecto_TFG.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Proyecto_TFG.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Background>
        <SolidColorBrush Color="CornflowerBlue" Opacity="0.5"/>
    </UserControl.Background>
    <StackPanel>
        <TextBlock Text="HOME VIEW" TextAlignment="Center" FontSize="40" FontWeight="SemiBold" Foreground="White"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Button Grid.ColumnSpan="2" Grid.Row="1"  VerticalAlignment="Center" Content="Outbounds" 
                 Width="180" FontSize="15" FontWeight="Bold" Foreground="White" 
                Command="{Binding UpdateViewCommandV2}" CommandParameter="outbound" Cursor="Hand">
                    <Button.Background>
                        <SolidColorBrush  Color="#FFBF0549" Opacity="0.5"/>
                    </Button.Background>
                </Button>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <ContentControl Content="{Binding SelectedViewModel}" Margin="30" />
            </StackPanel>
        </Grid>


    </StackPanel>

</UserControl>
